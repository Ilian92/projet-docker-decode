services:
  adminer:
    build:
      context: .
    container_name: adminer
    ports:
      - "8080:8080"
    networks:
      - symfony_network

networks:
  symfony_network:
    external: true
    name: symfony_network